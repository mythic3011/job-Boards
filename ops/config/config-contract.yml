{
  "version": 1,
  "defaults": {
    "STATE_DIR": ".blue-team-vm"
  },
  "derived": {
    "BT_STATE_DIR": "${STATE_DIR}",
    "PROMETHEUS_WEB_CONFIG_FILE": "${STATE_DIR}/rendered/prometheus.web-config.yml",
    "GRAFANA_DATASOURCES_FILE": "${STATE_DIR}/rendered/grafana.datasources.yml",
    "GRAFANA_ADMIN_SECRET_FILE": "${STATE_DIR}/runtime/grafana-admin-secret"
  }
}
