# ZAP baseline demo policy.
# Format: <plugin id><tab><level><tab><reason>
#
# These findings are informational scanner classifications for expected
# browser/session behavior or intentionally cached public assets. Keep them
# visible as INFO while preventing them from regressing the before/after WARN
# evidence lane.
10015	INFO	Expected cache-control review item; dynamic pages use explicit no-store and public static assets are cacheable by policy.
10049	INFO	Expected cacheability classification for no-store browser pages and cacheable static assets.
10111	INFO	/login is the intentional authentication endpoint.
10112	INFO	Laravel browser routes intentionally use HttpOnly session cookies for CSRF, auth, Livewire, installer, and flash-message flows.
