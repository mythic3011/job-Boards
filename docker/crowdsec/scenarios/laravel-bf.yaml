type: leaky
name: local/laravel-bf
description: "Detect brute force against Laravel login (invalid credentials responses)"
filter: >
  evt.Meta.log_type == 'http_access-log' &&
  evt.Parsed.verb == 'POST' &&
  evt.Parsed.uri contains '/login' &&
  (evt.Meta.http_status == '422' || evt.Meta.http_status == '401' || evt.Meta.http_status == '403')
groupby: evt.Meta.source_ip
capacity: 8
leakspeed: "30s"
blackhole: 30m
labels:
  confidence: 3
  spoofable: 0
  classification:
    - attack.T1110
  behavior: "http:bruteforce"
  label: "Laravel Login Bruteforce"
  service: http
  remediation: true
